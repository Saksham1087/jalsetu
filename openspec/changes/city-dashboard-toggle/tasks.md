## Tasks

- [x] 1. Add `viewMode` state (`'my'` | `'city'`) to Dashboard component, default `'my'`
- [x] 2. Build segmented pill toggle UI below welcome header — two tabs with active/inactive styling
- [x] 3. Conditionally toggle header text: "Welcome, {name}" in My View, "JalSetu Overview" in city view
- [x] 4. Compute stats from `myComplaints` or `complaints` based on viewMode
- [x] 5. Show Recent Complaints from correct data source per viewMode
- [x] 6. Hide empty state in city view — never show it when viewing city-wide data
- [x] 7. Verify `npm run build` and `npm run lint` pass cleanly
