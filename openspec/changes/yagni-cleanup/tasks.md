## Tasks

- [x] 1. Delete unused TrackPage.jsx component (redirected to dashboard)
- [x] 2. Remove dead `userLocation` parameter from useComplaints hook
- [x] 3. Remove dead `loading` return value + internal state from useNotifications
- [x] 4. Remove dead `isAuthenticated` from useAuth return
- [x] 5. Remove dead `onRefresh={()=>{}}` prop from AdminLayout's AdminDashboard usage
- [x] 6. Delete unused firestore.js exports: subscribeToNotifications, subscribeToSentNotifications, hardDeleteNotificationsByBatch
- [x] 7. Fix broken `fbError` reference in PublicMap.jsx (undefined variable)
- [x] 8. Update AGENTS.md — remove references to non-existent files
- [x] 9. Verify `npm run build` and `npm run lint` pass
